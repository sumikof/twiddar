name: my_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.0
  http: ^0.13.3
  oauth1: ^2.0.0
  url_launcher: any
  flutter_secure_storage: 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_native_splash: any

flutter:
  uses-material-design: true

flutter_native_splash: #https://yaba-blog.com/flutter-splash-screen/
  image: "assets/icon/logo.png" 
  color: "4DD0E1"